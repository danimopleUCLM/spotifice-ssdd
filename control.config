# Configuración para que el cliente localice el directorio
MediaDirectory.Proxy = directory:tcp -p 10002

# Configuración para la búsqueda (Hito 3)
IceStorm.TopicManager.Proxy = IceStorm/TopicManager:tcp -p 9999
Finder.TopicName = FinderTopic